root = "."
tmp_dir = "tmp"

[build]

cmd = "go build -o ./tmp/app ./cmd/main.go"
bin = "tmp/app"

include_ext = ["go", "tpl", "tmpl", "html"]

exclude_dir = ["vendor", "tmp"]

full_bin = "tmp/app"

[log]
time = true

[color]
main = "yellow"
watcher = "cyan"
build = "green"
runner = "magenta"
